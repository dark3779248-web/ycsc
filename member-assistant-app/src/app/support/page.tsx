'use client';
import { FormEvent,useEffect,useState } from 'react';import { createBrowserSupabase } from '@/lib/supabase/client';
type Msg={id:string;body:string;sender_type:string;created_at:string};
export default function Support(){const [conversation,setConversation]=useState<string|null>(null);const [messages,setMessages]=useState<Msg[]>([]);const [body,setBody]=useState('');const s=createBrowserSupabase();async function load(){const {data:{user}}=await s.auth.getUser();if(!user){location.href='/login';return;}let {data:c}=await s.from('support_conversations').select('id').eq('user_id',user.id).in('status',['open','pending']).order('created_at',{ascending:false}).limit(1).maybeSingle();if(!c){const {data:newC}=await s.from('support_conversations').insert({user_id:user.id,subject:'会员咨询'}).select('id').single();c=newC;}if(c){setConversation(c.id);const {data:m}=await s.from('support_messages').select('id,body,sender_type,created_at').eq('conversation_id',c.id).order('created_at');setMessages((m||[]) as Msg[]);}}useEffect(()=>{load();},[]);async function send(e:FormEvent){e.preventDefault();if(!conversation||!body.trim())return;const {data:{user}}=await s.auth.getUser();if(!user)return;await s.from('support_messages').insert({conversation_id:conversation,user_id:user.id,sender_type:'member',sender_user_id:user.id,body});setBody('');load();}return <main className="shell"><div className="top"><div className="brand">联系客服</div><a href="/">返回</a></div><div className="card"><div className="list">{messages.map(m=><div className="row" key={m.id}><div><strong>{m.sender_type==='member'?'我':'客服'}</strong><div>{m.body}</div></div></div>)}</div><form onSubmit={send} style={{display:'flex',gap:10,marginTop:18}}><input className="input" value={body} onChange={e=>setBody(e.target.value)} placeholder="请输入问题"/><button className="btn">发送</button></form></div></main>}
