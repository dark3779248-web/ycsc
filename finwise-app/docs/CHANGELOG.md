# 重要变更记录

### 统一资产显示单位

- 用户端余额、下单和持仓由 `FW` 改为 `USDT`。
- 界面明确标注为“模拟 USDT”，数据库字段结构保持不变。
- 原因：统一产品货币单位，同时避免误导用户认为已经接入真实链上资金。

## 2026-07-28

### Figma 界面实现

- 完成移动端主要页面和管理后台原型。
- 原因：先验证页面结构、导航和视觉方向。
- 注意：这一阶段的数据主要是演示数据。

### Vercel 与 GitHub

- GitHub 仓库：`dark3779248-web/ycsc`，生产分支：`main`。
- Vercel 项目根目录：`finwise-app`。
- 生产地址：`https://ycsc-chi.vercel.app/`。

### Supabase 第一阶段

- 新增用户资料、市场、订单、持仓、账本、通知和审计表。
- 为用户私有数据启用 RLS。
- 注册用户自动创建资料。
- 原因：把静态原型升级为具有真实账户和持久化数据的 MVP。

### 登录体验

- 添加用户名、邮箱、密码和确认密码注册。
- 添加邮箱免密码登录、验证提示和中文错误。
- 临时关闭邮箱确认。
- 原因：内置邮件服务测试额度低，开发阶段先验证账户和交易流程。

### 真实模拟积分下单

- 新增 `place_order` 数据库事务函数。
- 成功后写入订单、持仓、余额和账本。
- 余额不足、未登录、市场关闭等情况返回中文失败原因。
- 持仓页改为读取数据库，并支持“持仓中/已结算”切换。
- 原因：删除只显示成功提示但不保存数据的前端模拟行为。

## 记录规范

以后每次重要改动需要追加：日期、修改内容、修改原因、数据影响、回滚方式和审核结果。
